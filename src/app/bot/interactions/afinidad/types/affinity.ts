export type Affinity = {
    name: string,
    affinity: number,
}

export type MediaScore = {
    mediaId: number,
    score: number
}

export type SharedMedia = {
    id: number,
    scoreA: number,
    scoreB: number
}