type uRegistrado = {
    serverId: string,
    discordId: string,
    anilistUsername: string,
    anilistId: string
};

type sharedMedia = {
    id: number,
    scoreA: number,
    scoreB: number
}

export { uRegistrado, sharedMedia };